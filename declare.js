//one line object declaration 
const pen = { brand: 'econo', price: 10, color: 'black'};
const pencil = new Object(); //object declare 
console.log(pencil);

const rubber = Object.create({}); //empty object 
console.log(rubber);

//using class or function 
